function hello(name){
	if(typeof name === "undefined"){		// if the value type is undefined, return "Hello!"
		return "Hello!";
	} else {					// otherwise,
		return "Hello, " + name + "!";		// return ...
	}
}

function add(number, num){
	return number + num;
}

console.log(name);
